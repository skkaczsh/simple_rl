一阶段统一 whole‑body 运控底座技术方案研究

引言

这份“阶段一完整架构技术文档”阐述了一个在无视觉、无雷达、无完整世界模型的前提下构建能够长期扩展的人形机器人全身控制底座的设计思想。它建议从一开始就采用统一的全身控制器，通过轻量的预测头、可快速微调的本地适配器和联邦聚合来实现底层一致、上层灵活的控制架构。为了验证文档中的关键假设，本报告查阅了近期公开资料，对每个重要设计理由都给出了出处。

为什么坚持 headless / state‑based 训练

文档指出，在对现有硬件进行评估时，视觉任务消耗的显存远高于纯状态输入任务，因此阶段一应坚持无视觉。官方性能基准显示，在 Isaac-Velocity-Rough‑G1‑v0 环境中，以 4096 个环境运行 RSL‑RL 时需要大约 6.5 GB RAM 和 6.1 GB VRAM；而在 Isaac-Cartpole-RGB-Camera-Direct-v0 环境中，1024 个环境就需要 7.5 GB RAM 和 16.7 GB VRAM 。换句话说，带 RGB 相机的任务在显存方面的开销是纯状态任务的数倍。因此文档主张先训练高质量的全身控制器，再在后续版本接入视觉或世界模型。

NVIDIA 的推荐硬件也印证了这一选择。Isaac Lab 官方文档建议至少 32 GB 系统内存和 16 GB VRAM 。NVIDIA 开发者论坛进一步指出，Omniverse/Isaac Sim 的最小 GPU 为 RTX 3070 8 GB，而推荐是 RTX 4080 16 GB；为了高级用法和 Isaac Lab 训练，需要更多显存 。基于这些要求，文档认为 RTX 5070 Ti（16 GB VRAM）可以启动第一阶段的训练，但不适合同时处理视觉传感器和世界模型。

总体设计：统一控制核 + 四个外挂

文档提出的最小正确架构包括统一 whole‑body 控制核和四个外挂（小脑、前额叶、本地联邦、学校）：

1. 控制核：一个 unified actor‑critic，用遮罩命令输出全身指令。统一控制核的优势在于，现有研究（HOVER、HugWBC、TWIST）都证明单一控制器比为每个控制模式训练独立策略更通用。例如 HOVER 通过多模策略蒸馏将不同控制模式融合成统一策略，实现了模式间无缝切换 ；HugWBC 设计通用命令空间来支持走路、跳跃、站立等多种步态，并允许外部上肢干预 ；TWIST 利用 RL+BC 和未来动作条件，训练单一策略同时实现全身操控、行走和表现性动作 。
2. 小脑预测头：一个短时前向预测器，用 trunk 隐变量和命令预测未来几步的状态残差、接触概率和末端误差。文档强调这不是完整世界模型，而是利用短期动力学预测改善策略稳定性，类似于 Contrastive Forward Prediction 的思想。
3. 前额叶（本地适配器）：本地 episodic memory 与可快速调节的适配器。本地训练采用 AWAC / WSRL 等离线‑在线融合算法，只更新适配器而不更新共享参数。这样可以快速补偿每台机器人的延迟、执行器参数或载荷偏差。
4. 学校（联邦聚合层）：多节点通过联邦学习共享表示，并上传高价值经验片段。文档采用 PFedRL‑Rep 思路，即服务器学习共享特征表示，同时每个客户端保留个性化权重。PFedRL‑Rep 的公开论文指出该框架学习共同的特征表示并为每个客户端维护一个本地权重向量，从而在异质环境下获得更好泛化 。聚合器默认使用 FedAvg，对于异质性较强的情况切换到 SCAFFOLD；SCAFFOLD 通过引入控制变量纠正客户漂移，显著缓解非 iid 数据带来的收敛慢问题 。

图示结构中，只有当前 on‑policy rollout 存储在 GPU；所有长期经验都存放于 CPU/memmap/磁盘，利用 TorchRL 的 LazyMemmapStorage 在 CPU 端创建 memory‑mapped 存储。  显示默认设备为 CPU，并且不自动推断设备以防止误将数据放入 GPU 导致内存溢出。这支持文档中“经验不常驻显存”的设计。

技术栈选择的依据

仿真与主训练器

* Isaac Lab + RSL‑RL + PPO：RSL‑RL 是一种 GPU 加速的轻量学习库，支持多 GPU 训练并实现 PPO 和教师‑学生蒸馏等机器人相关算法 。搭配 Isaac Lab 可获得高并行的仿真环境，而且 Isaac Lab 提供 distillation runner、Hydra 配置系统等用于教师‑学生训练。
* 不将 IQL/AWAC 当作主训练器：AWAC（Advantage Weighted Actor Critic）旨在利用离线数据预训练并快速在线微调。BAIR 博文指出 AWAC 首先在离线数据上训练，然后通过在线交互快速调整策略 。其设计目标是利用过去数据加速新任务，而不是长时间稳定训练底座。IQL 是一种保守离线 RL 方法，通过避免对未见动作估值而保证安全 。IQL 论文强调该方法仅在离线数据中估值，能在离线初始化后通过在线微调提高性能 。因此文档建议主干训练仍使用强 on‑policy 方法（PPO+Distillation），而 AWAC/IQL 用于本地微调或离线 consolidation。
* WSRL 预热方法：近期工作 Warm‑Start RL 表明，在对离线策略进行在线微调时，不需要长期保留离线数据；通过用少量 rollouts 对 Q‑函数重新校准即可避免价值发散 。这验证了文档中“WSRL 说明在线微调只需少量 warmup rollouts”的说法。

经验与存储

* TorchRL Replay Buffer：文档采用 TorchRL 的 replay buffer，并指定使用 LazyMemmapStorage(device='cpu')。该 API 文档说明 LazyMemmapStorage 在 CPU 上存储 memory‑mapped tensordict，默认不自动推断设备以避免误入 GPU 。这样经验可以长期存在硬盘或 RAM 而不占用显存。
* PrioritizedSampler：TorchRL 文档解释 PrioritizedSampler 会为缓冲区的每个元素维护一个优先级；添加数据时优先级初始为 1，需要在训练过程中根据 TD 误差更新优先级 。这种按 TD 误差采样的机制是 Prioritized Experience Replay (PER) 的核心，提高了高价值样本的出现频率。
* Parquet 列式导出：Ray RLlib 离线 API 推荐使用 Parquet 作为离线数据的默认格式。官方文档指出，若数据采用 Parquet 格式（RLlib 推荐的离线数据格式），可以利用 Ray Data 的 read_parquet 方法进行列过滤和行过滤 。因此文档建议经验归档使用 Parquet，以便后续数据分析和离线训练。

联邦聚合

* PFedRL‑Rep：该框架学习共享特征表示和各客户端的个性化权重。OpenReview 摘要指出 PFedRL‑Rep 在异质环境下协同学习共享表示，同时为每个客户端维护本地权重向量 。
* SCAFFOLD：由于 FedAvg 在非 iid 数据上会产生客户端漂移，SCAFFOLD 提出利用控制变量来修正本地更新的 drift。论文证明该方法显著减少通信轮数并不受数据异质影响 。因此文档计划在出现严重漂移时切换到 SCAFFOLD。

统一接口（ABI）设计

文档详细设计了观测 obs_t、命令 cmd_t 和动作 act_t 的格式，力求在不同模块间保持一致：

* 观测包括驱动关节位置速度、IMU 信息、脚接触标志、上一次动作、当前和未来命令片段、命令掩码以及形态编码。历史堆叠数量建议为 4，避免过早引入重型 RNN。
* 命令采用统一的 masked unified command，其中骨盆高度、平面速度、转角速度和双腕未来 spline 轨迹均可按需开启；使用四个 knot（0.0 s–0.3 s）预测末端位置和六维旋转。
* 动作输出各关节期望位置增量、速度增量以及（可选）上肢 feed‑forward 扭矩，最终由本地 PD/伺服映射到实际命令。

这套接口与 HOVER 的多模式命令空间 、HugWBC 的通用命令空间 和 TWIST 的未来运动条件控制 一致，便于未来接入视觉或更高阶运动生成器。

模型结构与训练流程

形态编码器与共享主干

文档建议用每个关节/连杆的类型、长度、质量、限位等属性扁平化输入，经过小型 MLP 输出 32 维 morphology_token。这样能在后续升级到更复杂的 embodiment‑aware transformer 时保持 ABI 不变。共享主干采用 3 层 MLP (768→512→256)，激活函数为 ELU，并使用运行时均值/方差做归一化。

在头部设计上，文档建议使用两个子头（下肢头和上肢头）以及独立的 value head 和 predictor head。这样的轻度解耦避免上下肢互相干扰，同时仍共享 trunk；这与 HugWBC 在多步态中保持单一策略 以及 TWIST 用单一网络控制全身 的结论相符。

小脑预测头

预测头仅预测短期残差（骨盆高度、根部速度、脚接触、腕端误差）而非完整世界模型。文档认为短时动力学预测能提供物理先验和经验路由的得分，根据 Contrastive Forward Prediction 的研究，这可以提升行走控制的适应性。

本地适配器

本地适配器采用 FiLM 或 LoRA 等低秩调制器插入 trunk 和头部的早期层；客户端只更新适配器而保持共享参数不变。更新策略使用 AWAC 和行为克隆损失，并加上 KL 正则限制偏离全局策略。PFedRL‑Rep 提醒服务器只聚合表示，而客户端保留自适应层 。

训练流程

1. 阶段 A – 纯下肢教师：在高度、平面速度和 yaw 命令下训练统一控制器，使机器人能稳定行走/小跑。输出 teacher_loco.ckpt。
2. 阶段 B – 加入上肢轨迹：在维持行走稳定性的同时跟踪双腕 spline；先追踪位置，再打开姿态掩码。输出 teacher_wbc.ckpt。
3. 阶段 C – 启用预测头：训练小脑预测器，使 trunk 表征对动力学更敏感，并把预测误差用于经验路由和正则化。
4. 阶段 D – 学生蒸馏：使用 Isaac Lab 的 distillation runner 训练仅依赖部署可观测量的学生策略，通过动作 MSE 和价值蒸馏学习教师风格。
5. 阶段 E – 本地个性化与联邦：多个虚拟客户端对相同底座进行本地 AWAC 微调，上传高价值经验片段和适配器增量；服务器采用 FedAvg/SCAFFOLD 聚合共享参数。PFedRL‑Rep 提供理论依据：共享表示 + 个性化权重能够在异质环境下提升全局和局部表现 。

奖励与验收指标

文档设计奖励分主奖励和正则项，包括速度跟踪、骨盆高度、转角、腕端位置/姿态、存活奖励、滑动惩罚、动作变化、关节限位、能量等，并建议先保证下肢稳定后再逐渐增加上肢权重。验收指标包括：随机命令下跌倒率 <1%，骨盆高度 RMSE <3 cm，平面速度 RMSE <0.15 m/s，腕端 RMSE <6 cm 等，强调关注最差客户端而非平均奖励。

结论与下一步建议

文档提出的阶段一主线“Isaac Lab + RSL‑RL teacher/student + 小脑预测 + 本地 AWAC 微调 + PFedRL‑Rep 联邦聚合 + TorchRL CPU memmap + Parquet 存储”有以下优势：

1. 减少显存与内存负担：官方基准表明 state‑based 任务显存消耗远低于带相机任务 ，加之 LazyMemmapStorage 的 CPU 存储 ，保证经验不常驻显存。
2. 保证底座稳定性：使用成熟的 PPO/RSL‑RL 主干训练配合预测头和蒸馏，可继承 HOVER/HugWBC/TWIST 等研究的统一控制优势；避免过早引入离线 RL 主导算法。
3. 易于扩展：统一 command API 和形态编码器使策略在未来接入视觉输入或高层动作生成器时无需更改接口；PFedRL‑Rep 支持后续多客户端异构训练。 

下一步应将该文档转化为可执行的配置草案，包括 obs/action schema、奖励项实现、Hydra 配置树、联邦轮次设置和 Parquet schema，使团队能够基于此快速搭建阶段一的训练框架。

北极星扩展与改进方向

为使阶段一架构更具前瞻性和泛化能力，我们总结了若干基于最新研究的改进建议：

1. 形态表征升级：替代人工特征设计，可尝试将机器人形态编码成一串可学习的“序列令牌”，并使用 Transformer 等序列模型从强化学习目标中学习形态嵌入。这类方法在零样本泛化任务中比固定形态描述更具优势，并且无需预定义具体形态信息 。
2. 引入记忆网络和因果 Transformer：当前模型采用浅层 MLP，难以利用历史信息进行上下文适应。近期实验证明，利用历史观测–动作序列训练因果 Transformer 或小型 LSTM，可以在无权重更新的情况下根据历史状态调整行为，实现零样本从仿真到现实部署 。建议在统一控制核中探索带记忆的网络架构，以增强环境自适应性。
3. 拓展训练任务分布：为避免训练数据只覆盖有限的轨迹范围，建议引入随机采样的手臂末端位置和姿态目标，使训练分布均匀覆盖整个工作空间。Agility Robotics 的基础模型通过这种随机采样确保机器人能够精确到达任意位置 。
4. 尝试模型基强化学习：在纯模型自由 PPO 之外，可以引入基于动力学模型的学习框架，如在四足机器人实验中使用多步预测损失和模型预测控制仅用 4.5 分钟数据就实现行走 。这类方法能够显著提高样本效率，并易于迁移到新任务。
5. 融合模仿学习与扩散模型：模仿学习调查显示，行为克隆和 MPC 生成的数据占据主导地位，扩散模型正成为生成多样动作的前沿技术 。可在初始阶段引入专家演示或 MPC 轨迹，用于教师蒸馏和预训练，并尝试生成式模型构建丰富的上肢轨迹分布。
6. 强化联邦聚合策略：为进一步抵抗数据/系统异质性，可在 FedAvg 和 SCAFFOLD 之外尝试 FedProx 等方法。FedProx 在高度异质的数据集上通过引入近端约束显著提升了收敛稳定性和精度 。
7. 先进 RL 架构与教师蒸馏：科学机器人文章指出，结合教师 imitation 和大规模 RL 训练的因果 Transformer 在全尺寸人形机器人上取得了优于模型基控制器的性能，并能在现实地形中行走、适应扰动 。建议在训练流程中保留教师蒸馏，并探究类似 Transformer 的深层策略网络。

这些建议旨在为北极星架构提供下一阶段的扩展方向，使底座控制器在泛化能力、数据效率和多节点协同方面更具竞争力。

扩展讨论：43 DOF Unitree G1 与形态令牌

为什么选择 43 DOF G1

第一阶段文档中的仿真参考机器人是一台人形机器人，但没有指定具体硬件型号。根据 Unitree 官方文档，G1 基础版拥有 23 个自由度，其中单臂具有 5 个关节（肩、上臂和肘），单腿具有 6 个关节（髋、腿、膝和踝），腰部有 1 个关节 。G1‑EDU 版本在此基础上提供了灵巧手和额外手腕/腰部关节等可选升级，使机器人最多拥有 43 个自由度 。这个高自由度配置兼顾了行走、跑跳与复杂操作能力，适合作为统一 whole‑body 控制器的目标模型。因而在阶段一建议直接使用 43 DOF G1 模型进行仿真训练，并确保控制接口支持所有关节。

形态令牌的作用

文档当前为每个关节定义了手工特征（类型、长度、质量等）并通过小型 MLP 编码为“morphology token”，但最新研究表明，使用形态令牌（morphology tokens）将机器人设计表示为一串可学习的向量，可以无需手工构造描述便从强化学习目标中学习形态表示。AnyMorph 提出的 Transformer 架构通过处理形态令牌序列自动推断机器人的设计，实现了对未见形态的零样本泛化并在 benchmark 上获得 28%–32% 的性能提升 。Emergent Mind 对形态无关控制的综述也指出，AnyMorph 采用 token 序列代替具体的肢体结构，令策略通过 RL 目标学习各关节之间的关系和重要性 。这些形态令牌本身不含语义标签，而是由学习算法在训练过程中赋予意义。

在阶段一，可以将 G1 机器人的 43 个自由度映射为一个长度为 43 的形态令牌序列，令每个令牌对应一个关节或传感器。Transformer 或 MLP 会在训练中自动发现这些向量的关联和对任务的贡献，无需手工编写关节类别或几何参数。重要的是，这些“无语义令牌”不会在接入更复杂任务时被抛弃，而是作为统一接口贯穿整个控制体系：

* 统一控制核：形态令牌与观测状态共同输入，帮助策略根据不同机器人形态生成合适动作；
* 上层任务或世界模型：当未来需要引入视觉‑语言导航或完整世界模型时，可以将形态令牌与视觉特征、语言指令一起输入上层网络，让任务规划器知晓机器人有哪些自由度、可以执行哪些动作；
* 跨形态迁移：由于令牌序列的长度与自由度一致，同一策略可以在 23 DOF 和 43 DOF 之间共享权重，支持“少关节版本→多关节版本”的迁移。

因此，形态令牌不是一个过渡产物，而是整个北极星架构在扩展新任务和新硬件时的重要组成部分。

全流程 LoRA 与任务令牌

文档中提出在本地适配器上采用 LoRA 或 FiLM 等参数高效微调方法，只更新少量 adapter 而不改动共享参数。随着系统引入多模态世界模型或 VLN，这种策略依然有效。近年来的研究指出，参数高效微调（PEFT）能在保持基模型不变的前提下，通过低秩增量或前缀 token 快速适配新任务，仅使用 0.01%–1% 的参数就能显著提高性能  。例如，Morphology‑Aware Policy Transfer 工作在 Transformer 的注意力层和非线性层应用低秩 LoRA 模块，通过学习低秩矩阵 A 和 B 来改动权重矩阵，减少了可训练参数并提升适配效率 。

对高层任务可以采用 任务令牌（Task Tokens）。该方法在控制场景下训练一个任务特定的编码器（tokenizer），将观测映射为一组额外的 token 输入，而保持行为基础模型（BFM）冻结。论文中指出，任务令牌训练的 token 编码器能将训练所需的参数降低 125 倍，收敛速度提高 6 倍，并且由于基模型不变，可以与其他提示方式（如文本提示）兼容 。此外，它借鉴了 NLP 领域的 PEFT 技术，通过轻量模块调整模型行为，无需整体 fine‑tune 。因此当上层引入世界模型或 VLN 时，可以在基座之上添加任务令牌编码器，并使用 RL 训练这些 token 以满足新任务需求，同时保持底座的多样化控制能力和稳定性。

综合而言，对于未来扩展，建议：

1. 保留形态令牌：形态令牌在各层之间共享机器人形态信息，是实现机器人通用性的关键。
2. 本地适配器 + LoRA：底层控制器继续采用 LoRA/FiLM 等方法对特定机器人进行快速微调，满足不同延迟、执行器差异等需求。
3. 任务令牌适配器：在接入世界模型或 VLN 时，为上层模块引入任务令牌编码器，通过少量可训练参数适配复杂目标；这一做法既能利用预训练模型强大的生成能力，又能兼顾任务精度和收敛速度。
4. 全流程 PEFT：如果高层与底座之间需要更紧密的耦合，例如世界模型直接生成局部运动指令，则可以在连接层使用 LoRA，实现跨层低秩适配，减少重新训练整个网络的开销。

通过这些策略，北极星架构可以平滑地从纯状态控制过渡到多模态世界模型控制，同时确保不同层之间的接口和参数更新保持可管理、可扩展。

北极星最终愿景：三层思维架构与两层学习架构

本报告的前一部分聚焦于阶段一的“纯状态”底座方案，但用户的长期目标是构建一个类人的完整具身框架。类人性体现在三层思维架构和两层学习架构：

三层思维架构

1. 云端大脑（Cloud Brain）：运行在云端或数据中心的超大模型，包括视觉–语言世界模型、全局规划器和知识推理模块。云端大脑可以采用大规模开放权重模型（例如 Gemma‑4 31B、Qwen 27B 等），结合世界模型或多模态模型，以推理任务意图、场景理解和长时规划。云端大脑负责协调多台机器人、整合多源感知，并提供高层指令或未来轨迹给本地大脑。由于云端大脑资源充足，可承担完整强化学习、世界模型训练和复杂任务推理。
2. 本地大脑（Local Brain）：部署在机器人机载计算平台（如 Orin NX 16 GB）上的轻量化模型，负责实时感知、决策和控制。考虑到硬件约束，本地大脑运行经过全流程微调和量化后的 4 B 或 9 B 级视觉–语言模型（例如 Gemma‑4‑E4B 或 Qwen 9B），并集成统一 whole‑body 控制核。通过形态令牌  与命令空间，本地大脑能够在不同机器人形态之间共享知识并生成动作。为了适应多任务、多环境，本地大脑采用LoRA 或 FiLM等 PEFT 方法快速微调，这些方法只需调整基模型 0.01%–1% 的参数  ，从而在硬件受限的情况下实现在线更新。此外，本地大脑利用小脑预测头为动作提供短期动力学前瞻，并采用记忆网络或因果 Transformer 延伸历史信息，以提升适应性。
3. 跟随器小脑（Follower Cerebellum）：位于执行层的简单预测/滤波模块，负责解析来自本地大脑的命令并结合传感器反馈生成关节电机控制信号。它实现局部稳定控制（PD/阻抗控制），并可在极低延迟下对本地指令进行微调。小脑还可作为局部学习的输入，记录预测误差和接触事件用于经验路由。

两层学习架构

1. 本地在线混合推理/微调：本地大脑在执行任务的同时，通过缓冲区存储最新体验并使用 AWAC 或 WSRL 等算法进行少量在线更新  。与阶段一相同，本地大脑只更新适配器，而共享参数保持不变，从而保证在有限显存下的训练和稳定性。当高层模型（例如视觉–语言模型）需要更新时，通过 LoRA 或任务令牌的方式进行微调，保持基模型冻结  。
2. 联邦学习与学校聚合：多台机器人或仿真环境通过 PFedRL‑Rep 框架共享 trunk 表示，同时保留本地适配器 。每个客户端利用 Prioritized Sampler 按预测误差和 TD 误差筛选高价值体验 ，并上传到“学校”缓冲区。服务器使用 FedAvg、SCAFFOLD 或 FedProx 等聚合器  ，更新共享 trunk 和控制核；必要时将云端大脑的世界模型知识融入共享表示。这种层次化的联邦学习既保护了本地隐私，又能在异质任务和不同机器人之间共享底层控制技能。

技术边界与关键模块

* 硬件边界：本地大脑在 Orin NX 16 GB 上运行，为了在有限显存下实现在线推理和微调，需要使用 4 bit/8 bit 量化和 PEFT 等技术。虽然本报告无法提供具体硬件需求的文档引用，但业界共识认为，量化和 LoRA 可将 4 B–9 B 级模型压缩到消费级 GPU 可用范围。TurboQuant 等 KV cache 量化方法和新型向量量化算法有望进一步压缩内存占用并提升推理速度。
* 效率提升模块：在世界模型和语言模型方面，可以探索 Mamba‑3 等基于状态空间的序列模型用于长序列推理；其通过线性时间复杂度的状态更新获得比传统 Transformer 更高的推理效率（本报告未能找到可引用的公开文档）。类似地，TurboQuant 提供 KV 缓存量化策略和旋转技巧，可在几乎无损精度下显著减少 LLM 的缓存尺寸，这些技术将有助于在 Orin NX 上运行更大模型。
* 统一多模态融合：为实现类人具身智能，未来需要在本地大脑中融合视觉、语言、触觉等多模态信息。通过任务令牌和形态令牌的结合，世界模型可以将视觉/语言理解与机器人形态相联系，进而生成兼顾任务与动作约束的下游命令。

总体而言，这一三层思维架构和两层学习架构提供了从云端到本地的全流程技术蓝图：云端大脑负责全局感知和长时规划，本地大脑结合轻量化多模态模型与 unified whole‑body 控制器进行实时决策，跟随器小脑负责局部控制和预测；本地与联邦结合的学习机制既能保证单机适应性，又能利用集体经验不断进化控制核。此架构与阶段一底座方案兼容，并为未来接入世界模型、视觉导航或语言任务提供清晰的扩展路径。